<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Adam Dawkins" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <!-- IndieWeb related links -->
    <link rel="me" href="https://github.com/adamdawkins">
    <link rel="me" href="https://ruby.social/@adamdawkins">
    <link rel="webmention" href="https://webmention.io/adamdawkins.com/webmention">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <header class="site-header">
      <h1 class="p-author"><%= link_to "Adam Dawkins", root_path, rel: "author", class: "site-title" %></h1>
      <%= render "shared/navigation" %>
    </header>
    <%= render "shared/login" %>
    <main>
      <%= yield %>
    </main>
  </body>
</html>
